<p id="notice"><%= notice %></p>
  <%= image_tag @product.front_page.url(:original), class: ' image-fluid' %>
  <div class="field">
<br>
    <%=   @product.name %>
  </div>
    <div class="field">
   <br>
    <%=  @product.author %>
  </div>
    <div class="field">
    <br>
    <%=   @product.title %>
  </div>
    <div class="field">
   <br>
    <%=   @product.year %>
  </div>
    <div class="field">
   <br>
    <%=   @product.publisher %>
  </div>
    <div class="field">
   <br>
    <%=  @product.description %>
  </div>
    <div class="field">
   <br>
    <p> regular price <%= @product.price%></p> 
    <% @saleprice= @product.price-(@product.promotions.first.discount_amount/100*@product.price) %>
    <p> Sale Price: <%= @saleprice%> </p>
  </div>
  

 <%= form_for(@addproduct) do |f| %>
  
  
  <div class="field">
 
    <%= f.hidden_field :product_id, :value => @product.id %>
  </div>
  <div class="field">
    <%= f.hidden_field :product_id, :value => @product.id %>
  </div>
  <div class="field">
    <%= f.label :product_qty %><br>
    <%= f.number_field :product_qty %>
  </div>
   <div class="actions">
    <%= f.submit %>
  </div>
  <% end %>
<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
